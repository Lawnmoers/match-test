View match-test/data for the most recent data, or get the data yourself with the following commands:

Quick Start
Install Dependencies:

pip install aiohttp matplotlib
Run the Script:

python main.py
